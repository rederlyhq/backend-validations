{
    "type": "object",
    "properties": {
        "email": {
            "type": "string",
            "format": "email",
            "description": "The email to get a new password for.",
            "example": "test@rederly.com"
        }
    },
    "required": ["email", "password"],
    "additionalProperties": false
}