{
    "type": "object",
    "properties": {
        "email": {
            "type": "string",
            "format": "email",
            "description": "The email to login with.",
            "example": "test@rederly.com"
        },
        "password": {
            "type": "string",
            "description": "The password for the given email.",
            "example": "password",
            "minimum": 4,
            "maximum": 26
        }
    },
    "required": ["email", "password"],
    "additionalProperties": false
}