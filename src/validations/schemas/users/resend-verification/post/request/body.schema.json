{
    "type": "object",
    "properties": {
        "email": {
            "type": "string",
            "format": "email",
            "maxLength": 255,
            "description": "The email to send a verification token to.",
            "example": "test@rederly.com"
        }
    },
    "required": ["email"],
    "additionalProperties": false
}